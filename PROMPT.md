You are implementing the KT6 LUI-GUI Runtime project.

Business thought chains must be stored as executable playbooks under `playbooks/`.
Runtime loads playbooks, invokes Agent and Tools, emits events, and the frontend consumes those events to synchronize LUI and GUI.
